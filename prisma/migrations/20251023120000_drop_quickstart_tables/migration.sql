-- Drop Quickstart related tables (if they exist)
PRAGMA foreign_keys=OFF;

-- Use IF EXISTS to be safe across environments
DROP TABLE IF EXISTS "Quickstart";
DROP TABLE IF EXISTS "QuickstartProgress";

PRAGMA foreign_keys=ON;


